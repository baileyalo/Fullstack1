function CalculatePoints(numw, numd, numl){

    TeamPoints = (numw * 3 + numd ) + (numl*0);
    console.log (TeamPoints);
}

// call function 
CalculatePoints (3, 4, 2);
CalculatePoints (5, 0, 2);
CalculatePoints (0, 0, 2);